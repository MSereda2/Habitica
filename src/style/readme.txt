1. This module holds our application-level styles.
2. It can include theme definition (font, colours, typography) of the app UI, and global or commonly used styles.
3. Material UI’s Theme is defined here and it can have global styles also exported as a part of the theme object as shown below.
4. It can then be referenced in individual components using mui’s theme object.