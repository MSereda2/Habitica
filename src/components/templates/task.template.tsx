import React from 'react';
import { AppBar } from '../organisms/app-bar/app-bar.organism';

const TaskTemplate = () => {
    return (
        <div>
            <AppBar />
        </div>
    )
}

export { TaskTemplate };